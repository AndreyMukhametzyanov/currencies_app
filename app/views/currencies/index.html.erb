<div class = "col-sm-12 mt-3">
  <h5><p class="text-center"> Вся информация о валютах всего мира </h5></p>
</div>


<div class="row">
  <div class="col-sm-auto">
    <div class="row row-cols-2 row-cols-md-2 g-4">
      <% @currencies.each do |currency| %>
        <div class="col mb-4">
          <div class="card">
            <div class="card-body">
              <h5 class="card-title"><%= currency.name %></h5>
              <p class="card-text"><%= link_to 'Show about currency', currency_path(currency.char_code), class: 'btn btn-outline-primary' %></p>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>